# dappgo-stocks-shared — repo map

You're in **dappgo-stocks-shared** (Tier 4, pure TypeScript 5.9 — no React, no RN). Single source of truth for cross-app helpers: verdict labels, formatters, days-until math, insider-direction classifier, SEC form colors, MA computation, sector aggregation, ticker aggregation, contribution bars, percentage bars, chart axis math, commentary cleanup, summarize-ticker, dashboard summary, regime display, design tokens. ~23+ modules with matching `<name>.test.ts`. Tests use `ts-jest`. **Sibling repos**: linked into `~/git/dappgo-tw-stocks-app`, `~/git/dappgo-us-stocks-app`, `~/git/dappgo-options-app` via `file:` deps. After committing, run `~/git/dappgo-stocks-meta/scripts/sync-shared.sh` to mirror into apps' `src/utils/`. **Family entry**: `~/git/dappgo-stocks-meta/CLAUDE.md`.
